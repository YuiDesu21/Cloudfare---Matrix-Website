document.addEventListener("DOMContentLoaded",()=>{const id=sessionStorage.getItem("matrix_logged_in_member_id"),alertBox=document.getElementById("upgrade-alert"),layout=document.getElementById("upgrade-layout");if(!id||!window.MatrixDB){location.href="portal.html";return}const member=MatrixDB.getMemberById(id);if(!member){location.href="portal.html";return}if(member.status==="active"){show("Entry is already active on this account.","success");return}const pending=MatrixDB.getUpgradeRequests().find(item=>item.memberId===id&&item.status==="pending");layout.style.display="grid";const form=document.getElementById("upgrade-form"),submit=document.getElementById("upgrade-submit"),uplineCode=document.getElementById("upgrade-upline-code");if(pending){show(`Entry activation ${pending.referenceNumber} is waiting for administrator review.`,"info");submit.disabled=true;if(uplineCode)uplineCode.value=pending.matrixUplineCode||""}form.addEventListener("submit",event=>{event.preventDefault();if(!document.getElementById("upgrade-confirm").checked){show("Confirm the payment details before submitting.","danger");return}try{submit.disabled=true;MatrixDB.requestUpgrade(id,document.getElementById("upgrade-reference").value.trim(),uplineCode?uplineCode.value.trim().toUpperCase():"");show("Your Entry activation request was submitted for verification.","success")}catch(error){submit.disabled=false;show(error.message,"danger")}});function show(message,type){alertBox.className=`alert alert-${type}`;alertBox.textContent=message;alertBox.style.display="block"}});
